commit-msg:
  commands:
    commitlint:
      run: bunx --bun commitlint --edit {1}

pre-commit:
  commands:
    oxlint:
      glob: "src/**/*.ts"
      run: bunx oxlint {staged_files}
    typecheck:
      run: bun run typecheck
